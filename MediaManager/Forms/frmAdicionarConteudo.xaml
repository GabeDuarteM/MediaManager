<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:properties="clr-namespace:MediaManager.Properties"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="MediaManager.Forms.frmAdicionarConteudo"
        Title="{Binding AppName, Source={x:Static properties:Settings.Default}, StringFormat=Adicionar conteúdo - \{0\}}"
        Height="507" Width="829" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.Background>
        <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlColorKey}}" />
    </Window.Background>
    <Grid>
        <Image x:Name="imgBanner" Source="{Binding FanartUrl, Mode=TwoWay}" HorizontalAlignment="Center" Height="270" Width="790" Margin="10" VerticalAlignment="Top"  Stretch="UniformToFill">
        </Image>
        <Image Source="{Binding PosterUrl, Mode=TwoWay}" x:Name="imgPoster" HorizontalAlignment="Left" Height="188" Margin="30,55,0,0" VerticalAlignment="Top" Width="125" Stretch="Fill">
            <Image.Effect>
                <DropShadowEffect />
            </Image.Effect>
        </Image>
        <ComboBox x:Name="cboListaConteudo" ItemsSource="{Binding ResultPesquisa,Mode=TwoWay}" DisplayMemberPath="Title" SelectedItem="{Binding Video, Mode=TwoWay}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,289,0,0" Width="240" />
        <Button x:Name="btnConfig" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="254,289,0,0" Height="22" Width="25" Click="btnConfig_Click">
            <Image x:Name="imgGear" Height="15" Width="15" Source="/MediaManager;component/Resources/IMG_Gear.png" />
        </Button>
        <xctk:WatermarkTextBox x:Name="tbxPasta" Text="{Binding Video.FolderPath, Mode=TwoWay}" Watermark="Pasta..." ToolTip="Pasta..." VerticalAlignment="Top" HorizontalAlignment="Left" Height="22" Width="494" Margin="284,289,0,0" />
        <Button x:Name="btnPasta" Content="..." ToolTip="Selecionar..." HorizontalAlignment="Left" VerticalAlignment="Top" Margin="777,289,0,0" Height="22" Width="25" Click="btnPasta_Click" />
        <TextBox x:Name="tbxSinopse" Text="{Binding Video.Overview, Mode=TwoWay}" Margin="10,316,10,40" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" />
        <Button x:Name="btnCancelar" Content="Cancelar" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Width="75" IsCancel="True" />
        <Button x:Name="btnSalvar" Content="Salvar" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="90,10" Width="75" Click="btnSalvar_Click" IsDefault="True" />
    </Grid>
</Window>