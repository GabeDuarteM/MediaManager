<UserControl x:Class="MediaManager.View.ControlPoster"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Height="300" Width="200">
    <Grid Margin="15,15,7.5,7.5">
        <Grid.Effect>
            <DropShadowEffect />
        </Grid.Effect>
        <TextBlock>
            <Hyperlink Command="{Binding posterClick}" >
                <Hyperlink.Style>
                    <Style TargetType="Hyperlink">
                        <Setter Property="TextDecorations" Value="{x:Null}" />
                        <Setter Property="Cursor" Value="Arrow" />
                    </Style>
                </Hyperlink.Style>
            <Image Stretch="Fill" Source="{Binding PosterPath, Mode=TwoWay}" />
                </Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>
    
    